const MapPreview = () => {
  return (
    <div className="w-full h-64 bg-gray-300 flex items-center justify-center rounded-xl shadow-md">
      <span className="text-gray-700">Map Preview (Static for now)</span>
    </div>
  );
};

export default MapPreview;
